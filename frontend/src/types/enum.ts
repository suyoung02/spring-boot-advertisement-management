export enum Role {
  WARD = 'WARD',
  DISTRICT = 'DISTRICT',
  VHTT = 'VHTT',
}

export enum IS_ACTIVE {
  TRUE = 'TRUE',
  FALSE = 'FALSE',
}

export enum RequirementStatus {
  REJECT = 'REJECT',
  IN_PROGRESS = 'IN_PROGRESS',
  ACTIVE = 'ACTIVE',
}
